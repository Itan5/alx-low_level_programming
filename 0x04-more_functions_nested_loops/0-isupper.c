#include "main.h"
#include <stdio.h>

/**
 * _isupper - return 1 if upper and return 0 if lower
 *
 *@c: alphabet to be check
 * Return: Always 0 (successfull)
 */
int _isupper(int c)
{
	while 
		(c >= 65 && c <= 90)
		return (1);
	while
		(c < 65 && c > 90)
		return (0);
return (0);
}
