#include "main.h"
#include <stdio.h>

/**
 * _isdigit - Checks if an input is a digit or not
 *
 * @c: Inpute to be checked
 * Return: Always 0 (successful).
 */
int _isdigit(int c)
{
	while
		(c >= 48 && c <= 57)
			return (1);
	while
		(c < 48 && c > 57)
			return (0);
return (0);
}
