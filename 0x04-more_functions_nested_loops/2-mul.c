#include "main.h"
#include <stdio.h>

/**
 * mul - it multiplies two integers
 *
 *@a: first number to be multiplied
 *@b: second number to be multiplied
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
